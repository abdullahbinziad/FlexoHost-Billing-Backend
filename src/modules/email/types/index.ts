/**
 * Email module types - re-export from templates for cleaner imports
 */

export type {
    TemplateKey,
    EmailCategory,
    BrandProps,
    BaseEmailTemplate,
    SendResult,
} from '../templates/types';
